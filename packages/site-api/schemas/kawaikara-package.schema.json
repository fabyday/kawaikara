{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "kawaikara-package.schema.json",
  "title": "Kawaikara package.json Configuration",
  "description": "Adds Kawaikara-specific settings while allowing all standard package.json fields.",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "kawaikara": {
      "description": "Kawaikara Bundle tooling configuration.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "development": {
          "description": "Settings used when Kawaikara hosts this project directly.",
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "outputDirectory": {
              "description": "Generated revision directory relative to this project. It must not overlap sourceDirectory or a watched directory.",
              "$ref": "definitions.schema.json#/definitions/safeRelativePath",
              "default": ".kawaikara/development"
            }
          }
        }
      }
    }
  }
}
